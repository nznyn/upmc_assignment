#function f(x) finds max value and location
#of the highest value in 3D array.

import numpy as np

#create a 3D array "x" of random integers
x = np.random.randint(0, 99, (5, 5, 5))

#print the array
print(x)


def f(x):
  print(np.amax(x))
  print(np.where(x == np.amax(x)))

f(x)
